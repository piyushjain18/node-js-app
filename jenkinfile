node {
    checkout scm

    docker.withRegistry('onlypiyushjain/nodejsapp', 'dockerhubcreds') {

        def customImage = docker.build("onlypiyushjain:nodejsapp")

        /* Push the container to the custom Registry */
        customImage.push()
    }
}
